services:
  rag-api:
    image: nvidia-rag-api
    ports:
      - "8000:8000"
    environment:
      NVIDIA_API_KEY: ${NVIDIA_API_KEY}
    volumes:
      - ./cache:/app/cache
      - ./data:/app/data
    restart: unless-stopped
